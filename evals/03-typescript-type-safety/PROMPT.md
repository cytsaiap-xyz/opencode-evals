# Fix TypeScript Type Errors

The file `types.ts` contains several functions with type errors that prevent compilation. Fix all the type errors while maintaining the intended functionality.

## Your Task

Fix all TypeScript type errors in `types.ts`. Do NOT use `any` or `as` type assertions — use proper type narrowing, generics, and TypeScript features instead.
