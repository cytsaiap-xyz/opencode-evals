# Write Unit Tests

The file `math.ts` contains several utility functions. Write comprehensive vitest tests in a new file `math.test.ts`.

## Your Task

Create `math.test.ts` with tests that cover:
- Normal/expected inputs for each function
- Edge cases (empty arrays, zero, negative numbers)
- At least 3 test cases per function
- Use `describe` blocks to group tests by function
